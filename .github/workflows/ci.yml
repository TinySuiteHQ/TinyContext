name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  core-boundary:
    name: Core package boundary
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install the base package
        run: |
          python -m pip install --upgrade pip
          python -m pip install .

      - name: Verify core imports without declared server dependencies
        run: |
          python -c "import tinycontext; from tinycontext import MemoryInput, TinyContextConfig, recall_memories, save_memories"
          python -c "import importlib.metadata as m; r=m.requires('tinysuite-context') or []; assert not any(x.lower().startswith(('fastapi','uvicorn','mcp')) and 'extra ==' not in x.lower() for x in r)"

      - name: Run core contract tests
        run: python -m unittest tests.test_public_config_and_paths tests.test_memory_service tests.test_memory_recall_pipeline tests.test_memory_store_service

  platform-tests:
    name: ${{ matrix.os }} / Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
        python-version:
          - "3.12"
          - "3.13"
          - "3.14"

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install package with server extra
        run: |
          python -m pip install --upgrade pip
          python -m pip install ".[server]"

      - name: Run full test suite
        run: python -m unittest discover tests

      - name: Smoke-test MCP over stdio
        run: python scripts/smoke_mcp_stdio.py

  package:
    name: Build and install distributions
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Check release metadata
        run: python scripts/check_release_metadata.py

      - name: Build wheel and source distribution
        run: |
          python -m pip install --upgrade pip build twine
          python -m build
          python -m twine check dist/*

      - name: Install wheel with server extra into a fresh environment
        run: |
          python -m venv "${RUNNER_TEMP}/tinycontext-wheel"
          WHEEL="$(find "${GITHUB_WORKSPACE}/dist" -name '*.whl' -print -quit)"
          "${RUNNER_TEMP}/tinycontext-wheel/bin/python" -m pip install "${WHEEL}[server]"

      - name: Verify installed wheel away from the checkout
        working-directory: ${{ runner.temp }}
        run: |
          "${RUNNER_TEMP}/tinycontext-wheel/bin/python" -c "import importlib.metadata as m, json, os, tinycontext, tomllib; expected=tomllib.load(open(os.path.join(os.environ['GITHUB_WORKSPACE'], 'pyproject.toml'), 'rb'))['project']['version']; assert m.version('tinysuite-context') == expected; json.dumps(tinycontext.TinyContextConfig().to_dict())"
          "${RUNNER_TEMP}/tinycontext-wheel/bin/python" "${GITHUB_WORKSPACE}/scripts/smoke_mcp_stdio.py"

      - name: Upload distributions
        uses: actions/upload-artifact@v4
        with:
          name: python-package-distributions
          path: dist/
          if-no-files-found: error

  docker-build:
    name: Docker build smoke test
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build MCP image
        uses: docker/build-push-action@v6
        with:
          context: .
          load: true
          push: false
          tags: tinycontext:test
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Smoke-test image entrypoint
        run: docker run --rm tinycontext:test tinycontext --help
